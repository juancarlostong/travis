language: minimal
os: linux

env:
  global:
    count: 0
    REPEAT: 20

script:
  - cat /etc/resolv.conf
  - while docker build . --no-cache; do (( count++ )); echo "count=$count"; if [[ $count -gt $REPEAT ]]; then break; fi; done
